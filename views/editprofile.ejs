<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-300 p-8">
    <div class="max-w-5xl mx-auto">
        <div class="bg-white shadow-lg rounded-lg p-6 mb-8">
            <h2 class="text-3xl font-semibold text-gray-800 mb-4">Edit Profile</h2>
            <form action="/api/profile/<%= profile._id %>/edit" method="post">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    
                    <div>
                        <label for="firstName" class="text-md font-medium">First Name</label>
                        <input type="text" id="firstName" name="firstName" value="<%= profile.firstName %>" placeholder="Enter first name" class="mt-2 p-3 w-full border rounded-md">
                    </div>
            
                    <div>
                        <label for="lastName" class="text-md font-medium">Last Name</label>
                        <input type="text" id="lastName" name="lastName" value="<%= profile.lastName %>" placeholder="Enter last name" class="mt-2 p-3 w-full border rounded-md">
                    </div>
    
                    <div>
                        <label for="email" class="text-md font-medium">Email</label>
                        <input type="email" id="email" name="email" value="<%= profile.email %>" placeholder="Enter email" class="mt-2 p-3 w-full border rounded-md">
                    </div>
                </div>
            
                <div class="mt-6">
                    <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white px-5 py-3 rounded-md font-medium">Update Profile</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>